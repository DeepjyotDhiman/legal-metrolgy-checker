# TriNetra Backend

Please refer to the main [Root README](../README.md) for full architecture, installation, testing, and extension documentation.

### Quick Commands:
```bash
# Run migrations
alembic upgrade head

# Run tests
pytest -v

# Run dev server
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```
